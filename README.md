Build instructions:

  - Copy in a subdirectory of your grub2 checkout.
    For example, "grub-read-file-to-env".

  - Export GRUB_CONTRIB environment variable to point to this directory.

  - Build GRUB as usual.
